#!/usr/bin/env sh

if [ -z "$WAYLAND_DISPLAY" ];
then
    rofi -show window \
        -show-icons \
        -theme theme/windowmenu.rasi
else
    swaywindow -a "$(swaywindow -w | rofi -dmenu -theme theme/windowmenu.rasi -show-icons | sed -e 's|\(.*\)\t.*$|\1|')"
fi
